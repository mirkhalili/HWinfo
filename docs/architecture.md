# معماری
Presentation در public/index.php، منطق دامنه در Services/Core و persistence در PDO/SQL قرار دارد. عملیات import در تراکنش واحد انجام می‌شود تا ثبت batch، asset و audit اتمیک باشد. داده سخت‌افزاری متغیر در raw_json نگهداری و فیلدهای هویتی/مالکیت/وضعیت نرمال می‌شوند.
